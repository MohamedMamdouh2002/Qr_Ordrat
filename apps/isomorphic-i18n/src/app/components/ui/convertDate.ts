// import 'moment/locale/ar';
// import moment from 'moment';

// export default function convertDate(date: string, locale?: 'ar' | 'en'){
//     moment.locale(locale)
//     return moment(date).fromNow()
// }